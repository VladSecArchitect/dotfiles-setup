# Check the window size after each command
shopt -s checkwinsize

# Enable `**` to match directories recursively
shopt -s globstar

# Append to the history file instead of overwriting it
shopt -s histappend
